# cosyte

Open-source healthcare-standard parsers (`@cosyte/*`) and the tooling around them.

This repository holds the org's shared GitHub Actions workflows. See the individual `@cosyte/*` repos
for the parsers themselves.
